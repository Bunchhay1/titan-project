titan-prometheus:
    image: prom/prometheus:latest
    container_name: titan-prometheus
    # 🔥 ត្រូវតែបន្ថែមបន្ទាត់នេះជាដាច់ខាត:
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    networks:
      - titan-network
    restart: always

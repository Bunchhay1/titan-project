FROM postgres:15

# Install PostGIS on arm64-compatible postgres:15 (Debian Bookworm base)
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        postgresql-15-postgis-3 \
        postgresql-15-postgis-3-scripts && \
    rm -rf /var/lib/apt/lists/*
